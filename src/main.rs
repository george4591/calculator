mod stack;
use stack::Stack;
use std::string;

fn main() {
//     let mut stack: Stack<isize> = Stack::new();
//     stack.push(1);
//     stack.push(2);
//     let item = stack.pop();
//     assert_eq!(item.unwrap(), 2);
//     println!("{}", item.unwrap());
    // let mut string = String::from("(AAA]");
    // string = convert_brackets(&string);
    // println!("{}", string);
}
